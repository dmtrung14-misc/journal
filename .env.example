# Authentication (Required)
ADMIN_PASSWORD_HASH=
JWT_SECRET=

# Cloudinary (Required for production, optional for local development)
# Sign up at cloudinary.com (free tier: 25GB storage, 25GB bandwidth/month)
# Get credentials from: Dashboard â†’ Settings
# Create an unsigned upload preset named 'ml_default' in Settings â†’ Upload â†’ Upload presets
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
